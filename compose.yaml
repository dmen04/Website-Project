services:
  js-app:
    build:
      context: .
      dockerfile: Dockerfile
    image: js-app:latest
    container_name: js-app
    restart: unless-stopped
    init: true
    ports:
      - "3000:3000" # Exposed by Dockerfile
    # env_file: ./.env  # Uncomment if .env file exists
    # Add depends_on here if you add external services
    # networks: [appnet]  # Uncomment if you add more services/networks

# No external services detected in README, Dockerfile, or source code
# No volumes section needed as there are no persistent data requirements
# No networks section needed for a single service
